apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: taskmanager
data:
  ENVIRONMENT: "production"
  ALLOWED_ORIGINS: "https://taskmanager.example.com"
  LOG_LEVEL: "INFO"
  CACHE_TTL_SECONDS: "60"
