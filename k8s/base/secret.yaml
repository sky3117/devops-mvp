# NEVER commit real secrets to git. This is a TEMPLATE.
# In real usage: use `kubectl create secret generic` from CI/CD,
# or better - use Sealed Secrets / External Secrets Operator / AWS Secrets Manager.
apiVersion: v1
kind: Secret
metadata:
  name: backend-secrets
  namespace: taskmanager
type: Opaque
stringData:
  DATABASE_URL: "postgresql://taskuser:CHANGE_ME@postgres:5432/taskdb"
  REDIS_URL: "redis://redis:6379/0"
  SECRET_KEY: "CHANGE_ME_GENERATE_WITH_openssl_rand_hex_32"
