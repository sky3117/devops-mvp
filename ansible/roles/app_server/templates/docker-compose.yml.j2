version: "3.9"
services:
  backend:
    image: ghcr.io/YOUR_GH_USERNAME/taskmanager-backend:latest
    restart: unless-stopped
    env_file: .env
    ports: ["8000:8000"]
  frontend:
    image: ghcr.io/YOUR_GH_USERNAME/taskmanager-frontend:latest
    restart: unless-stopped
    ports: ["3000:3000"]
